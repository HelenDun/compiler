void sampleTwo (int x)
{
    float[10] b;

    while (x<10)
    {
        x=x-1;
    }
    x=8;
}

void main ()
{
    string[10] a;
    int w;

    w=3;
    sampleTwo(w);
}

// doesn't like this one only